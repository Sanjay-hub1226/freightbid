{
  "name": "freightbid-platform",
  "version": "1.0.0",
  "description": "FreightBid Reverse Bidding Transport Platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend",
    "db:migrate": "node backend/scripts/migrate.js",
    "db:seed": "node backend/scripts/seed.js",
    "setup": "npm run install:all && npm run db:migrate && npm run db:seed",
    "deploy:build": "npm run install:all && npm run build"
  },
  "engines": { "node": ">=18.0.0" }
}
